int main()
{
type
Student {
  str name;
  int marks;
}
endtype
decl
  int temp;
  Student a;
enddecl

begin
  intialize();
  a = alloc();
  a.name = "John";
  a.marks = 89;
  temp = exposcall( "Write", -2, a.name);
  temp = exposcall( "Write", -2, a.marks);
  retval = free(a);
  return 0;
end
}
